<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cust.system.repository.DictRepository">
    <sql id="tableName">
        SYS_DICT
    </sql>

    <sql id="tableAlias">
        <include refid="tableName"/>
        o
    </sql>

    <sql id="columns">
        o.ID AS id,
        o.CREATED_BY AS createdBy,
        o.CREATED_DATE AS createdDate,
        o.LAST_MODIFIED_BY AS lastModifiedBy,
        o.LAST_MODIFIED_DATE AS lastModifiedDate,
        o.ACTIVATED AS activated,
        o.LABEL AS label,
        o.TYPE AS type,
        o.VALUE AS value,
        o.DESCRIPTION AS description,
        o.REMARK AS remark
    </sql>

    <sql id="joinTables">
    </sql>

    <sql id="joinColumns">
    </sql>

    <sql id="conditions">
    </sql>

    <select id="findAllByType" parameterType="java.lang.String" resultType="com.cust.system.domain.Dict">
        SELECT
        <include refid="columns"/>
        <include refid="joinColumns"/>
        FROM
        <include refid="tableAlias"/>
        <include refid="joinTables"/>
        WHERE
        o.TYPE = #{type}
        AND o.ACTIVATED = '1'
        ORDER BY o.ID
    </select>


</mapper>
