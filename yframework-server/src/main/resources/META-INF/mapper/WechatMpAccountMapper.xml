<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.becypress.wechat.repository.WechatMpAccountRepository">
    <sql id="tableName">
        WECHAT_MP_ACCOUNT
    </sql>

    <sql id="tableAlias">
        <include refid="tableName"/>
        o
    </sql>

    <sql id="columns">
    </sql>

    <sql id="joinTables">
    </sql>

    <sql id="joinColumns">
    </sql>

    <sql id="conditions">
    </sql>

    <select id="findOneByAppId" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM
        <include refid="tableAlias"/>
        WHERE app_id =#{appId}
    </select>

</mapper>
