<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cust.system.repository.FileInfoRepository">
    <sql id="tableName">
        SYS_FILE_INFO
    </sql>

    <sql id="tableAlias">
        <include refid="tableName"/>
        o
    </sql>

    <sql id="columns">
        o.ID AS id,
        o.CREATED_BY AS createdBy,
        o.CREATED_DATE AS createdDate,
        o.LAST_MODIFIED_BY AS lastModifiedBy,
        o.LAST_MODIFIED_DATE AS lastModifiedDate,
        o.ACTIVATED AS activated,
        o.file_name AS fileName,
        o.store_name AS storeName,
        o.path AS path,
        o.url AS url,
        o.md5 AS md5
    </sql>

    <sql id="joinTables">
    </sql>

    <sql id="joinColumns">
    </sql>

    <sql id="conditions">
    </sql>


</mapper>
