<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.becypress.wechat.repository.WechatMpConfigRepository">
    <sql id="tableName">
        wechat_mp_config
    </sql>

    <sql id="tableAlias">
        <include refid="tableName"/>
        o
    </sql>

    <sql id="columns">
        o.ID AS id,
        o.name AS name,
        o.api_key AS apiKey,
        o.api_secret AS apiSecret,
        o.token AS token,
        o.aes_key AS aesKey,
        o.oauth2redirect_uri AS oauth2redirectUri
    </sql>

    <sql id="joinTables">
    </sql>

    <sql id="joinColumns">
    </sql>

    <sql id="conditions">
    </sql>

    <!--<select id="findOneByApiKey" parameterType="java.lang.String"-->
    <!--resultType="com.becypress.wechat.domain.WechatMpConfig">-->
    <!--SELECT-->
    <!--<include refid="columns"/>-->
    <!--FROM-->
    <!--<include refid="tableAlias"/>-->
    <!--WHERE-->
    <!--o.app_key = #{apiKey}-->
    <!--</select>-->

    <!--<select id="findOneByAppId" parameterType="java.lang.String"-->
    <!--resultType="com.becypress.wechat.domain.WechatMpConfig">-->
    <!--SELECT-->
    <!--<include refid="columns"/>-->
    <!--FROM-->
    <!--<include refid="tableAlias"/>-->
    <!--WHERE-->
    <!--o.app_id = #{appId}-->
    <!--</select>-->

</mapper>
